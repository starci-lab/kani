/** Params for the OPEN POSITION PREPARE step. */
export interface OpenPositionPrepareStepParams {
    /** Bot ID. */
    botId: string
    /** Liquidity pool ID. */
    liquidityPoolId: string
    /** Job ID. */
    jobId: string
}

/** Params for the OPEN POSITION PLAN step. */
export interface OpenPositionPlanParams {
    /** Bot ID. */
    botId: string
    /** Liquidity pool ID. */
    liquidityPoolId: string
    /** Job ID. */
    jobId: string
}