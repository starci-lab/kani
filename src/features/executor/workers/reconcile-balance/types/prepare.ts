import type {
    ProcessParams, ProcessResult 
} from "./process"

/** Params for the PREPARE phase. */
export type PrepareParams = ProcessParams

/** Result of the PREPARE phase. */
export type PrepareResult = ProcessResult
