/** Alphabet used for numeric OTP codes. */
export const NUMBERS_ALPHABET = "0123456789"

/** Length of generated OTP code. */
export const OTP_CODE_LENGTH = 6

/** Default length of nanoid suffix in generated codes (e.g. prefix-xxxxxxxxxx). */
export const CODE_NANOID_LENGTH = 10
