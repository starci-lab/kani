/** Message for socket.io client id not found. */
export interface SocketIoClientIdNotFoundMessage {
    botId: string
}

/** Message for socket.io client not found. */
export interface SocketIoClientNotFoundMessage {
    botId: string
}