/** IoRedis/Valkey instance key. */
export enum IoRedisInstanceKey {
    Cache = "cache",
    BullMQ = "bullmq",
    LockAuthority = "lock-authority",
    Throttler = "throttler",
    Adapter = "adapter",
}
