/**
 * WebSocket URL for Gate.io stream API.
 */
export const GATE_WS_URL = "wss://api.gateio.ws/ws/v4/"

/**
 * Stream name identifier for Gate.io last price updates.
 */
export const GATE_LAST_PRICE_STREAM_NAME = "gate-last-price"

/**
 * Stream name identifier for Gate.io order book updates.
 */
export const GATE_ORDER_BOOK_STREAM_NAME = "gate-order-book"
